{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "go build -tags netgo -ldflags '-s -w' -o indexServer cmd/indexServer/main.go"
  },
  "deploy": {
    "startCommand": "./indexServer"
  }
}
