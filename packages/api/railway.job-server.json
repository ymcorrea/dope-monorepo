{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "go build -tags netgo -ldflags '-s -w' -o jobServer cmd/jobServer/main.go"
  },
  "deploy": {
    "startCommand": "./jobServer"
  }
}
