#!/bin/sh

# Uses "go watch" to reload server on file changes
# https://github.com/mitranim/gow
# (to install run `go install github.com/mitranim/gow@latest`)
cd "$(dirname "$0")/.."
gow run cmd/indexServer/main.go