{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "go build -tags netgo -ldflags '-s -w' -o apiServer cmd/apiServer/main.go"
  },
  "deploy": {
    "startCommand": "./apiServer"
  }
}

